version: '3.7'
services:
  postgresql:
    image: postgres:12-alpine
    ports:
      - '3306:3306'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=user1
      - POSTGRES_PASSWORD=password1do
